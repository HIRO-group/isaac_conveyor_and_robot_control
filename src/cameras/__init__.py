"""Sim cameras: Replicator-rendered camera prims published over Zenoh.

Import submodules directly (`from cameras.rig import CameraRig`) once the app
is up; `rig.py` needs a live `SimulationApp`.
"""
